package leetcode

func ThreeSum(nums []int) [][]int {
	//seem array of nums size and true if index consitues the solution
	//same brut force approch first
	//3 loops
	//i to find i index
	//j and k to do the same as well

	//append when condition matches and return the array at the end
}
